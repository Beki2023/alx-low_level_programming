#include <stdio.h>

/**
 * main - no argument
 * print the last digit of the given random number
 *
 * Return: zero
 */

int main(void)
{
	int i;
	for (int i = 'a'; i <= 'z'; i++)
		putchar(tolower(i));

	return (0);
}
