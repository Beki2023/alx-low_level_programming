#include <stdio.h>

/**
 * main - no argument
 * print the the all lowercase exept e and q
 *
 * Return: zero
 */

int main(void)
{
	int i;

	for (i = '0'; i <= '9'; i++)
		if (i <= '9')
		putchar(i);
		else
			putchar();
	return (0);
}
