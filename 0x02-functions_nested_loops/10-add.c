#include "main.c"
/**
 * add - adding two number
 * @i: the number to be added
 * @j: the second number
 *
 * Return: the rusult of the added value
 */
int add(int i, int j)
{
	int result = i + j;

	return (result);
}
